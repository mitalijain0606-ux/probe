name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    env:
      DATABASE_URL: postgresql://uho:uho_password@localhost:5432/url_health_test
      JWT_SECRET: ci-test-secret-key-that-is-at-least-32-chars

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: uho
          POSTGRES_PASSWORD: uho_password
          POSTGRES_DB: url_health_test
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U uho"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - run: npm install
      - run: npx prisma generate
      - run: npx prisma migrate deploy
      - run: npm run typecheck
      - run: npm run lint
      - run: npm test
      - run: npm run build

  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - run: npm install
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run build
